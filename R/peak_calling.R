result =exomePeak2(bam_ip = IP_BAM,
           bam_input = INPUT_BAM,
           gff_dir = GENE_ANNO_GTF,
           genome = "mm10",
           consistent_peak = TRUE,
           paired_end = FALSE)
